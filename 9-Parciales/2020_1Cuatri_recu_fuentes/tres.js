function mostrar() {
 

let nombre;
let edad;
let sexo;
let estado;
let terminar = "si";

while (terminar = "si"){
	
}













































	
/*
	let nombre;
  let edad;
  let sexo;
  let estado;
  let temperatura;
  let tempMax = 0;
  let mayViudo = 0;
  let contViudo = 0;
  let adultsolviu = 0;
  let contadorsoltviud = 0;
  let maytemp = 0;
  let contmaytemp = 0;
  let sumaSolt = 0;
  let hSolt = 0;
  let terminar = "si";
  let Nom;
 
  while (terminar == "si") {
    nombre = prompt("Ingrese Nombre");
    edad = parseInt(prompt("Ingrese edad"));
    sexo = prompt("Ingrese sexo m (masculino), f (femenino)");
    estado = prompt("ingrese estado civil soltero, casado o viudo");
    temperatura = parseFloat(prompt("Ingrese temperatura"));
    terminar = prompt("Ingrese si para ingresar otra persona");
    if (temperatura > tempMax) {
      tempMax = temperatura;
      Nom = nombre;
    }
    if (estado == "viudo" && edad >= 18) {
      mayViudo = contViudo + 1;
    }

    switch (estado) {
      case "soltero":
      case "viudo":
        adultsolviu = contadorsoltviud + 1;
        break;
    }
    if (edad >= 60 && temperatura >= 38) {
      maytemp = contmaytemp + 1;
    }
    switch (estado) {
      case "soltero":
        sumaSolt += edad;
        break;
    }
  }
  hSolt = sumaSolt / 2;
  alert(Nom + " tine temperatura " + tempMax);
  alert("Hay " + mayViudo + "viudos");
  alert("Hay " + adultsolviu + " solteros y viudo");
  alert(
    "Hay " + maytemp + " personas de tercera edad con temperatura mayor a 38"
  );
  alert("promedio de edad a de solteros: " + hSolt);
  */
}
